[pytest]
testpaths = tests
# the repo uses flat modules at the root rather than an installed package,
# so put the root and scripts/ on sys.path instead of hacking it in conftest
pythonpath = . scripts
